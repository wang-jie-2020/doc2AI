
---
## 🤖 Assistant

你说得对，我上一个回答把“微信”误解成了“企业微信”，而且也没有明确说明这些群机器人 Webhook 是**单向推送**。更准确地说：

## 1. Webhook 本身不是双向通道

例如：

```text
本机 Agent ──HTTP POST──> 飞书/企业微信 Webhook ──> 群消息
```

这个 Webhook 只负责“外部系统向平台发送消息”，平台不会通过这个 Webhook 把用户消息回传给你的 Agent。

要实现双向交互，需要两条链路：

```text
本机 Agent ──发送消息 API──> 平台
本机 Agent <──事件订阅/回调/WebSocket── 平台
```

飞书的**群自定义机器人**明确只支持向群内单向推送，不能接收或响应用户消息。[1][2][3]

---

# 2. 飞书：可以实现真正双向

不要使用“群自定义机器人 Webhook”，而应该创建一个**飞书应用机器人**。

架构如下：

```text
飞书用户
   │
   │ 发送消息 / @机器人
   ▼
飞书应用机器人
   │
   │ 事件订阅
   ▼
本机 Agent
   │
   │ 调用飞书发送消息 API
   ▼
飞书用户或群聊
```

## 方案 A：使用飞书长连接，适合本机 Agent

飞书支持通过 SDK 建立 WebSocket 长连接：

```text
本机 Agent ──主动建立 WebSocket──> 飞书开放平台
本机 Agent <──事件推送──────────── 飞书
```

这种方式通常不需要把本机 HTTP 端口暴露到公网。飞书开放平台会通过长连接向本机推送事件。[4]

适合场景：

- 在飞书中给 Agent 发命令；
- 用户 @Agent；
- Agent 接收消息后执行任务；
- Agent 将结果回复到飞书；
- Agent 运行在个人电脑、服务器或内网机器上。

## 方案 B：使用公网 HTTPS 回调

也可以让飞书请求你的回调地址：

```text
飞书 ──HTTPS POST──> https://your-domain.example.com/feishu/events
本机 Agent ──API──> 飞书
```

要求：

- 本机服务有公网可访问地址；
- 使用 HTTPS；
- 处理飞书的 URL 校验；
- 验证事件签名或加解密；
- 快速返回事件接收成功；
- 再异步交给 Agent 处理。

如果本机没有公网地址，优先使用飞书长连接。

## 飞书应用机器人需要配置

大致步骤：

1. 在飞书开放平台创建自建应用；
2. 开启机器人能力；
3. 申请消息相关权限；
4. 配置“事件与回调”；
5. 订阅接收消息事件；
6. 配置长连接或 HTTPS 回调；
7. 创建并发布应用版本；
8. 将机器人添加到群聊或与其单聊。

应用机器人可以接收用户消息，并通过消息 API 回复；这与自定义机器人完全不同。[1][2]

---

# 3. 个人微信：没有官方通用的双向 Webhook

如果你说的是普通的**个人微信号**，那么结论是：

> **个人微信没有面向普通开发者开放的、官方支持的机器人 Webhook/API，可以让本机 Agent 与个人微信进行双向收发消息。**

因此不存在类似下面这种官方方案：

```text
个人微信 ──官方 Webhook──> 本机 Agent
本机 Agent ──官方 API────> 个人微信
```

也就是说，不能像飞书应用机器人那样，注册一个个人微信机器人，然后配置回调地址实现双向通信。

## 官方可行的微信相关方案

### 方案一：微信公众号

如果你可以接受“公众号”而不是个人微信号，可以使用：

```text
用户 ──> 公众号
          │
          │ 微信服务器回调
          ▼
      本机/云端 Agent
          │
          │ 客服消息或模板/订阅消息
          ▼
        用户微信
```

但它有一些限制：

- 用户需要关注公众号；
- 不是从个人微信号发消息；
- 消息类型和回复时效受公众号规则限制；
- 主动向用户发送消息需要符合公众号接口和业务规则；
- 本机通常需要公网 HTTPS 回调，或者使用中转服务。

### 方案二：微信小程序订阅消息

适合任务通知、状态提醒等场景：

```text
用户授权订阅
       ▲
       │
Agent ──> 微信小程序订阅消息
```

但它不是聊天机器人，通常不能作为任意双向对话通道。

### 方案三：非官方个人微信自动化

网上常见的方案包括：

- Windows PC 客户端自动化；
- `wxauto` 一类桌面自动化；
- 基于微信协议的机器人框架；
- Hook、协议逆向或第三方机器人框架；
- 手机端辅助操作。

这类方案本质上不是官方 Webhook，而是：

```text
个人微信客户端
       ▲
       │ 自动化/协议层
       ▼
本机 Agent
```

它们可能实现：

- 监听个人微信消息；
- 自动回复；
- 给好友或群发送消息；
- 将微信消息转发给本地 Agent。

但风险比较明显：

- 微信版本升级后可能失效；
- 可能触发登录限制、风控或封号；
- 部分方案涉及逆向、Hook 或协议模拟；
- 不适合承载重要业务；
- 不建议用于大规模或商业化机器人。

如果只是个人实验，可以把它当作“桌面自动化集成”，不能把它当作稳定的官方 API。

---

# 4. 如果只是给个人微信发送 Agent 通知

如果需求是：

```text
Agent 完成任务
    ↓
提醒到你的个人微信
```

可以考虑以下替代路径：

```text
Agent
 ├──> 公众号模板/订阅消息
 ├──> 小程序订阅消息
 ├──> PushPlus / Server酱 等微信通知服务
 ├──> 邮件/短信/系统通知
 └──> 非官方个人微信自动化
```

这类服务通常是：

```text
Agent ──HTTP POST──> 通知服务 ──> 微信消息/公众号通知
```

它们可以实现“发到微信”，但不等于“绑定个人微信账号的双向机器人”。

---

# 5. 推荐架构

## 如果需要稳定双向对话

优先选择：

```text
飞书应用机器人 + 长连接
```

架构：

```text
飞书
  ⇅ WebSocket / 消息 API
本机 Agent
```

## 如果必须使用个人微信

分情况：

| 需求 | 推荐方案 |
|---|---|
| 只接收任务完成通知 | 公众号、订阅消息、PushPlus/Server酱 |
| 与 Agent 双向聊天 | 非官方微信桌面自动化，但有封号和维护风险 |
| 稳定的企业级双向机器人 | 飞书应用机器人、钉钉应用机器人、企业微信应用 |
| 不希望开放本机端口 | 飞书长连接、主动轮询、中转服务 |
| 必须是个人微信号 | 官方方案基本不可得 |

所以，严格回答你的问题：

- **飞书自定义机器人 Webhook：单向，不能双向。**
- **飞书应用机器人：可以双向，使用事件订阅/长连接 + 消息 API。**
- **个人微信：没有官方通用的双向 Webhook；只能使用公众号/小程序等官方产品，或承担风险使用非官方客户端自动化。**
[1] [N/A](https://open.feishu.cn/document/server-docs/im-v1/faq?lang=zh-CN)

[2] [N/A](https://open.feishu.cn/document/client-docs/bot-v3/bot-overview?lang=zh-CN)

[3] [自定义机器人使用指南- 开发文档- 飞书开放平台](https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot?lang=zh-CN)

[4] [N/A](https://open.feishu.cn/document/event-subscription-guide/callback-subscription/callback-overview?lang=zh-CN)

[5] [N/A](https://open.feishu.cn/document/ukTMukTMukTM/ucTM5YjL3ETO24yNxkjN?lang=zh-CN)